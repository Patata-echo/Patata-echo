[project]
name = "resume"
version = "0.1.0"
description = "Resume of Echo"
dependencies = [
    "pyyaml>=6.0",
    "jinja2>=3.1.2",
    "marko>=1.2.2",
    "requests>=2.28.2",
]
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pdm.scripts]
build = {call = "renderer:build()"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]